namespace variaveis
{
    class Program
    {
        static void Main()
        { //-Tipos de variaveis
           
            /*
            int: números inteiros (32 bits)
            float: números de ponto flutuante (32 bits)
            double: números de ponto flutuante (64 bits)
            decimal: Valores monetários (128 bits)
            string: Para sequências de caracteres (16 bits por caractere)
            char: Para somente um caractere (16 bits)
            bool: valor boleano (true ou false)
            */

            //Exemplo
            string nome = "Tchali";
            int inteiro = 17;
            double real = 1.70;
            char caracter = 'a';
            bool boleano = true;

        }
    }
}